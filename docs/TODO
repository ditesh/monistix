TODO

1a. Add distinguishing of services which monitor external machines

2. Add check for diskspace <= 5% (optionally to not write to disk if no space)

3. Add the following plugins:

a) Sendmail
b) Courier
c) Squid
d)

4. Test "tail" behaviour in postfix profile

5. Add tests which:

a) Test invalid configuration
b) Test unreachable hosts
c) Test all expected keys exist
d) Test values of keys are within correct bounds
